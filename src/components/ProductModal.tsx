import React from "react";


interface Details {
  isOpen: boolean;
  toggleModal: () => void;
}
const ProductModal = (probs: Details) => {
  return (
    <>
      {/* <Modal isOpen={probs.isOpen}></Modal> */}
    </>
  );
};

export default ProductModal;
